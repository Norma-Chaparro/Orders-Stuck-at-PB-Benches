# Orders-Stuck-at-PB-Benches
This query finds orders stuck at PB (pack bench) screens instead of valid station IDs (e.g., DPB1:04). Orders must be linked to the actual station ID, not the PB screen (e.g., pb04), or they cannot be packed out.
